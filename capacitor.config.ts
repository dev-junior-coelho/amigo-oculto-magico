import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.juniorcoelho.amigooculto',
  appName: 'Amigo Oculto Mágico',
  webDir: 'dist'
};

export default config;
